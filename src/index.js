import './index.html';
import './index.scss';

document.addEventListener('DOMContentLoaded', () => {

    const aboutBtn = document.querySelector('.yellow-btn');

    aboutBtn.addEventListener('click', () => {
        console.log('Webpack is working!');
    });


})



